# Live Quiz App

- React/Vite client lives in `client`.
- Express/Socket.io server lives in `server`.
- Run `npm run dev` from the project root.
- Keep scoring and phase transitions authoritative on the server.
